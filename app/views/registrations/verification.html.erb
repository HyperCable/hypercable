<div class="bg-white max-w-md w-full mx-auto shadow-md rounded px-8 pt-6 pb-8 mb-4 mt-8">
  <h2 class="text-xl font-black">Success!</h2>
  <div class="my-4 leading-tight">
    We've sent an activation link to <b><%= current_user.email %></b>. Please click on the link to activate your account.
  </div>
  <div class="mt-8 text-sm">
    Didn't receive an email?
  </div>
  <div class="mt-2 text-sm text-gray-600 leading-tight">
    Please check your spam folder and contact <a href="mailto:hoooopo@gmail.com">hoooopo@gmail.com</a> if the problem persists
  </div>
  <div class="mt-8 text-sm">
    <% if Rails.env.development? %>
      <%= link_to 'Verify email for development env', verify_registrations_path(token: current_user.email_verification_token) %>
    <% end %>
  </div>
</div>